<view class="block">
	<view class="pic">
		<image class="picimg" src="{{aimg}}" mode="asaspectFill"></image>
	</view>
	<view class="blockitem">
		<view class="left">
			<view class="name">
				{{title}}（1日）
			</view>
			<view class="jiezhi">报名截止时间{{signup_end}}</view>
		</view>
		<view class="right">
			<view wx:if='{{type2}}' class="canyu">{{'68/100人参与'}}</view>
			<view class="btn">{{text}}</view>
		</view>
	</view>
</view>